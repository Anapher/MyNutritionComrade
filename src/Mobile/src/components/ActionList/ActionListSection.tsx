import React from 'react';

export type ActionListSectionProps = {
   name: string;
   children: React.ReactNode;
   renderHeader?: () => React.ReactNode;
};

export default function ActionListSection(props: ActionListSectionProps) {
   return null;
}
